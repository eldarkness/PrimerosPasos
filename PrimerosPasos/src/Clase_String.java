
public class Clase_String {

	public static void main(String[] args) {
		
		/* Metodos de clase String
		 * length() -> Devuelve la longitud de una cadena de caracteres
		 * charAt(n) -> Nos devuleve la posicion de un caractere especificado en el parentesis. Empieza a contar desde 0
		 * substring(x,y) -> Nos devuelve una subcadena dentro de la cadena empezando por el caracter x hasta el y
		 * equals(cadena) -> Compara dos cadenas y nos devuelve un valor boolean. Distingue entre mayusculas y minusculas
		 * equalsIgnoreCase(cadena) -> Lo mismo que el anterior pero no distingue entre mayusculas y minusculas
		 * 
		 */

		
		
		
	}

}
